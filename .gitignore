target
.arch_data
.arch_local
app.log
new_txns.txt
new_blocks.txt
authority.key
user.key
.DS_Store
.env

# Deploy tooling: gitignored key directories (autara-deploy). NEVER commit keys.
.keys-testnet/
.keys-mainnet/
.keys-*/

# Failing-case seeds proptest writes out on failure; local artifacts, not sources.
proptest-regressions/
