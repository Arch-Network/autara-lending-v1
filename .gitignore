target
.arch_data
.arch_local
app.log
new_txns.txt
new_blocks.txt
authority.key
user.key
.DS_Store
.env

# Deploy tooling: gitignored key directories (autara-deploy). NEVER commit keys.
.keys-testnet/
.keys-mainnet/
.keys-*/

# Local planning artifacts must never be committed.
/docs/superpowers/

# Rollback ELFs dumped by backup_program_elf — large binaries, never committed.
/backups/
